// Home route — replace with your actual landing UI.
export default function Home() {
  return (
    <main>
      <h1>{{ project-name }}</h1>
      <p>Replace this page with your real UI. Filled in during /build.</p>
    </main>
  );
}
