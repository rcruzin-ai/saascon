# Task plan — calorie-tracker (v1)

> Generated by `/plan`. Vertical slices derived from `template/SPEC.md`. Each task is shippable, verifiable, and ends in a working `localhost:3000` demo + commit.

## Conventions

- **Vertical slices.** Each task touches schema → data → UI as needed to deliver one user-visible behavior.
- **Per-slice gate** (CLAUDE.md §3): typecheck + lint + build, fresh-DB smoke test (`rm template/local.db*` then dev), hand-math any totals, commit `T-00X: <title>`, tick `tasks/todo.md`.
- **Pause checkpoints:** after T-001 (schema swap — DB badge must re-green) and after T-003 (quick-add — riskiest UX moment).
- **Working directory:** all `npm` commands run from `template/`. The harness cwd is the repo root.
- **Out-of-plan discoveries** go below as `OOP-N` — never silently expand scope.

## Dependency graph

```
T-001 (schema + ER diagram)              ← blocking root
   │
   ├──► T-002 (today read view + dev seed)
   │       │
   │       ├──► T-003 (quick-add form)        ← CHECKPOINT
   │       │       │
   │       │       └──► T-007 (delete entry)
   │       │
   │       ├──► T-004 (foods library + re-log)
   │       │
   │       ├──► T-005 (settings + target progress bar)
   │       │
   │       └──► T-006 (history view + CSS bars)   needs only T-001 + a way to
   │                                              read entries; runs after T-002
   │                                              for nav cohesion
```

T-004, T-005, T-006 are **parallelizable in principle** after T-002 — but they share `tasks/todo.md` and the per-slice commit cadence, so we ship them sequentially in the order above unless asked otherwise.

## Tasks

### T-001 — Schema swap: foods + entries + settings (with dual migrations)

- **Goal:** drop the saascon `examples` placeholder; introduce the three real tables. Home-page DB badge re-greens against the new schema. ER diagram updated.
- **Files touched:**
  - `template/db/sqlite/migrations/<ts>__drop_examples.sql` (new)
  - `template/db/sqlite/migrations/<ts+1>__create_calorie_tracker.sql` (new)
  - `template/db/migrations/<ts>__drop_examples.sql` (new, Postgres mirror)
  - `template/db/migrations/<ts+1>__create_calorie_tracker.sql` (new, Postgres mirror with RLS + anon policies)
  - `template/db/sqlite/schema.sql` (rewrite to reflect new tables)
  - `template/db/schema.sql` (rewrite to reflect new tables, RLS-enabled)
  - `template/src/app/page.tsx` (point `ensureSeed` + badge at `foods` table instead of `examples`, or simply at the seeded `settings` row)
  - `template/src/lib/supabase/health.ts` (read: change probe target from `examples` → `settings`; one-line table swap)
  - `template/docs/er-diagram.md` (Mermaid: foods 1—∞ entries; settings standalone; FK food_id nullable on delete set null)
- **DB shape (SQLite dialect, authoritative for local mode):**
  - `foods (id text pk, name text not null, calories integer not null, protein_g real, carbs_g real, fat_g real, created_at text default current_timestamp)` + `unique index foods_name_lower on foods (lower(name))`
  - `entries (id text pk, food_id text references foods(id) on delete set null, name_snapshot text not null, calories_snapshot integer not null, protein_snapshot real, carbs_snapshot real, fat_snapshot real, logged_at text default current_timestamp)` + `index entries_logged_at on entries (logged_at desc)`
  - `settings (id integer pk check (id = 1), daily_calorie_target integer not null default 2000)` + seed row `insert or ignore into settings (id) values (1)`
- **Acceptance criteria:**
  - [ ] Both migration trees have sibling files with matching timestamp prefixes (`ls db/migrations db/sqlite/migrations` shows pairs).
  - [ ] `db/sqlite/schema.sql` and `db/schema.sql` rewritten — no `examples` references in either; both list the three new tables verbatim.
  - [ ] Postgres migration enables RLS on every table and grants `anon` read where the v1 surface needs it (foods read, entries read, settings read; writes go through the app — leave write policies for a future cloud-mode slice).
  - [ ] `rm template/local.db*` then `npm run dev` → first request applies migrations, badge is **green** ("local SQLite connected · settings table reachable · 1 row").
  - [ ] `template/docs/er-diagram.md` shows the three entities + FK.
  - [ ] No string interpolation in any new SQL identifier path (manual read of new files).
- **Verification:**
  - `cd template && rm -f local.db local.db-shm local.db-wal && npm run dev` → curl `http://localhost:3000` → grep response for `bg-green-500` and `connected`.
  - `npm run typecheck && npm run lint && npm run build` → all green.
- **Dependencies:** none.
- **Status:** ☑ done
- **CHECKPOINT after this slice — pause for user review.**

### T-002 — Today read view + dev seed

- **Goal:** render `/` as the today-view shell — entries logged today, running totals, progress bar against `daily_calorie_target`. No write path yet; reads only. Idempotent dev seed plants 2–3 entries so the page has something to render on a fresh DB.
- **Files touched:**
  - `template/src/lib/db/sqlite.ts` (export a thin `getSqliteConnection()` so product code can use prepared statements directly — playbook §4)
  - `template/src/lib/db/queries.ts` (new) — `getEntriesForToday()`, `getTodayTotals()`, `getDailyTarget()`. Today boundary built from `new Date()` local parts → UTC ISO strings, passed via `?` placeholders.
  - `template/src/lib/db/seed-dev.ts` (new) — idempotent: only seeds when `entries` is empty AND only first call per process. Wrapped in `conn.transaction(...)`. Inserts 2–3 foods + 2–3 entries dated "today".
  - `template/src/app/page.tsx` (rewrite): server component, calls `seedDev()` then `getEntriesForToday()` + `getTodayTotals()` + `getDailyTarget()`; renders header (totals + progress bar), then list of entries, then a placeholder for the quick-add form ("Quick-add coming next slice"). Mobile-first wrapper `mx-auto flex min-h-screen max-w-md flex-col gap-6 p-4 md:p-6`. DB health badge moves to a small footer line so it stays visible.
  - `template/src/components/progress-bar.tsx` (new) — server component, takes `value` + `target` props, renders bar with `role="progressbar"`, `aria-valuenow/min/max`, color shift past 100%, `tabular-nums` numeric column.
- **Acceptance criteria:**
  - [ ] Fresh DB → `/` shows seeded entries with timestamps (HH:MM local), running totals (calories + 3 macros), progress bar with `aria-valuenow` matching the totals number.
  - [ ] DB badge stays green (now reading `settings`, but page renders even if seed is a no-op).
  - [ ] Today boundary: an entry forced into yesterday's local-day does NOT appear (manual test via `node -e` insert with a `logged_at` set to yesterday's UTC equivalent).
  - [ ] Page is a Server Component — `grep '"use client"' src/app/page.tsx src/components/progress-bar.tsx` returns zero.
  - [ ] Build size for `/` ≤ 170 B First Load JS (no client components).
- **Verification:**
  - Hand-math: sum the seeded entries' calories on paper → matches the totals row.
  - Mobile viewport (DevTools 375 px): totals + progress bar + first entry visible without scrolling. **State explicitly** that this is a JSX read (no headless browser available) and the wrapper is `max-w-md`, totals are above the entries list.
  - Per-slice gate: typecheck + lint + build green; commit `T-002: today read view + dev seed`.
- **Dependencies:** T-001.
- **Status:** ☑ done

### T-003 — Quick-add form (the one `'use client'` surface)

- **Goal:** add the quick-add form to `/` as a single client component using `useActionState` (React 19). Submitting upserts the food by `lower(name)` and inserts an `entries` row with snapshot fields. Inline error message, success clears the form via redirect/`useActionState` reset.
- **Files touched:**
  - `template/src/app/actions.ts` (new) — `"use server"`. `createEntry(prev, formData)`. Parses `name` (1–80, trimmed), `calories` (int 0–10000), optional `protein_g` / `carbs_g` / `fat_g` (real 0–1000). On invalid → return `{ error: "..." }` for `useActionState`. On valid → upsert food via `on conflict (lower(name)) do update set calories=excluded.calories, protein_g=excluded.protein_g, carbs_g=excluded.carbs_g, fat_g=excluded.fat_g returning id`, then insert `entries` row with `name_snapshot/calories_snapshot/...` copied from the just-upserted food. `revalidatePath("/")`.
  - `template/src/components/quick-add-form.tsx` (new) — `"use client"`. `useActionState(createEntry, { error: null })`. Inputs: name (`maxLength=80`), calories (`inputMode="numeric"`, `pattern="[0-9]*"`), protein/carbs/fat (`inputMode="decimal"`). Each input wrapped in `<label>`. Submit button `min-h-11`. `<p role="alert">` for the error.
  - `template/src/app/page.tsx` — render `<QuickAddForm />` in place of the placeholder. Form goes above the entries list (above-the-fold priority).
  - `template/src/lib/db/queries.ts` — add `upsertFoodAndLogEntry(input)` helper used by the action so the SQL lives next to the other product queries.
- **Acceptance criteria:**
  - [ ] Adding "oatmeal · 320 kcal · 10g protein" via the form → entry appears at top of today's list, totals update, progress bar advances.
  - [ ] Adding "Oatmeal" again with different calories → no duplicate row in `foods` (verify with `select count(*) from foods where lower(name)='oatmeal'` → 1). `foods.calories` updated to the new value (latest write wins). New `entries` row uses the **new** snapshot values.
  - [ ] Validation: empty name → inline error, no DB write. Calories = "abc" → inline error. Calories = 50000 → inline error.
  - [ ] Build size for `/` ≤ 1.3 kB First Load JS (only the form is `'use client'`).
  - [ ] `grep -nE 'prepare\(.*\$\{|prepare\(.*\+' src/lib/db src/app/actions.ts` → zero hits.
  - [ ] No `select`-then-`insert/update` race — the upsert is one statement (playbook §4).
- **Verification:**
  - Validation parity test (playbook §3): replicate `parseQuickAdd` in a `tsx` script under `template/`, run a table of edge cases (empty, max-len, negative, fractional calories, NaN), assert results.
  - DB-direct test: call `upsertFoodAndLogEntry()` from `node -e` via `tsx`, verify rows in both tables.
  - Per-slice gate; commit `T-003: quick-add form (one 'use client' surface)`.
- **Dependencies:** T-002.
- **Status:** ☑ done
- **CHECKPOINT after this slice — riskiest UX moment, pause for user review.**

### T-004 — Foods library + re-log

- **Goal:** new route `/foods`. Server-rendered list of every food in the library (most-recent-`created_at` first). Each row has a "Log again" server-action button — one tap re-logs that food with current macros, redirects back to `/foods`.
- **Files touched:**
  - `template/src/app/foods/page.tsx` (new) — server component. Reads via `listFoods()`. Renders `<form action={relogFood}>` per row with hidden `food_id` input. Page wrapper matches the today-view shell. Header link "← Today" back to `/`.
  - `template/src/app/page.tsx` — add nav link "Foods library →" near the top.
  - `template/src/app/actions.ts` — add `relogFood(formData)` server action: reads `food_id`, looks up food, inserts `entries` row with snapshot copied from the food's current values, `revalidatePath("/")` and `revalidatePath("/foods")`.
  - `template/src/lib/db/queries.ts` — add `listFoods()` and `getFoodById()` (or fold the lookup into `relogFromFoodId()` to keep one query).
- **Acceptance criteria:**
  - [ ] `/foods` renders one row per food with name + calories + macros + "Log again" button.
  - [ ] Tapping "Log again" → entry appears on `/` today list with current snapshot values; no duplicate food row created.
  - [ ] Re-log path is **one tap from `/foods`** (success metric Q3a). `/foods` is one tap from `/`. Total: 2 taps from home → matches success metric.
  - [ ] Page is a Server Component — `grep '"use client"' src/app/foods/page.tsx` returns zero.
  - [ ] Build size for `/foods` ≤ 170 B First Load JS.
- **Verification:** seed 3 foods, click re-log on each, verify three new entries; per-slice gate; commit `T-004: foods library + re-log`.
- **Dependencies:** T-003.
- **Status:** ☑ done

### T-005 — Settings + target progress bar live update

- **Goal:** new route `/settings`. Server-component form (no new `'use client'`) that updates `settings.daily_calorie_target`. Redirects to `/settings?ok=1` on success, `/settings?error=invalid` on bad input. The progress bar on `/` already reads `daily_calorie_target` from T-002 — just verify it updates.
- **Files touched:**
  - `template/src/app/settings/page.tsx` (new) — server component. Reads current target. Renders form with single `daily_calorie_target` integer input + submit. Conditional `<p role="status">Saved.</p>` if `?ok=1`; conditional `<p role="alert">…</p>` if `?error=invalid`.
  - `template/src/app/actions.ts` — `updateDailyTarget(formData)` server action: parses integer 500–10000, on invalid `redirect("/settings?error=invalid")`, on valid update + `redirect("/settings?ok=1")`. `revalidatePath("/")` before redirect.
  - `template/src/lib/db/queries.ts` — `setDailyTarget(value)`.
  - `template/src/app/page.tsx` — add nav link "Settings →" alongside "Foods library →".
- **Acceptance criteria:**
  - [ ] Updating target on `/settings` → return to `/` shows the progress-bar denominator changed.
  - [ ] Invalid input (0, 100000, "abc") → `?error=invalid`, no DB write.
  - [ ] No new `'use client'` files (`grep -r '"use client"' src/app/settings` returns nothing).
  - [ ] Build size for `/settings` ≤ 170 B First Load JS.
- **Verification:** per-slice gate; commit `T-005: settings + daily target`.
- **Dependencies:** T-002 (the progress bar exists). Independent of T-003 / T-004.
- **Status:** ☑ done

### T-006 — History view (last 7 days + CSS bars)

- **Goal:** new route `/history`. Last 7 days, one row per day, totals (calories + 3 macros) + a CSS bar showing calories vs current `daily_calorie_target`. **Single aggregate query** — `group by date(logged_at, 'localtime')` — then JS-side fill of empty days.
- **Files touched:**
  - `template/src/app/history/page.tsx` (new) — server component. Calls `getHistoryLast7Days()` + `getDailyTarget()`. Renders 7 rows newest-first with totals and a `<div role="progressbar">` colored bar.
  - `template/src/lib/db/queries.ts` — `getHistoryLast7Days()`. ONE `prepare(...).all(...)` call. SQL: `select date(logged_at, 'localtime') as d, sum(calories_snapshot) as cal, sum(coalesce(protein_snapshot,0)) as p, sum(coalesce(carbs_snapshot,0)) as c, sum(coalesce(fat_snapshot,0)) as f from entries where date(logged_at, 'localtime') >= ? group by d`. JS fills missing dates from `[today-6, today]` with zeros.
  - `template/src/app/page.tsx` — add nav link "History →".
- **Acceptance criteria:**
  - [ ] Page renders exactly 7 rows even with sparse data (gaps filled with zeros).
  - [ ] Bar width = `min(100, total/target * 100)`%. Past 100% the bar is colored differently (e.g. red vs green) — accessible color, not color-only signal (also show the number).
  - [ ] **Single aggregate query** — `grep -c '\.prepare(' src/lib/db/queries.ts` inside the `getHistoryLast7Days` function body returns 1 (verify via line range).
  - [ ] Build size for `/history` ≤ 170 B First Load JS.
- **Verification:**
  - Hand-math: seed entries across 3 distinct days, sum calories per day on paper, compare to rendered values.
  - Per-slice gate; commit `T-006: history view + CSS bars`.
- **Dependencies:** T-001 (schema), T-002 (queries module). Doesn't need T-003/T-004/T-005, but ships after them for nav cohesion.
- **Status:** ☑ done

### T-007 — Delete entry

- **Goal:** per-row "Delete" button on the today view. Server-action, no confirm modal in v1 (per spec). Removes the entries row by id, `revalidatePath("/")`.
- **Files touched:**
  - `template/src/app/page.tsx` — add a `<form action={deleteEntry}>` per entry row, with hidden `id` input and a small icon-only button (`aria-label="Delete entry"`, `min-h-11 min-w-11`).
  - `template/src/app/actions.ts` — `deleteEntry(formData)` server action.
  - `template/src/lib/db/queries.ts` — `deleteEntryById(id)`.
- **Acceptance criteria:**
  - [ ] Tapping delete removes the row immediately on next render; totals and progress bar update.
  - [ ] Deleting a non-existent id is a no-op (no error UI, no crash). Validate in the action with a numeric/uuid sanity check at the boundary.
  - [ ] No new `'use client'` files.
  - [ ] Build size for `/` unchanged from T-005 (still ≤ 1.3 kB — only the quick-add form is client).
- **Verification:** seed, delete, observe totals decrement; per-slice gate; commit `T-007: delete entry`.
- **Dependencies:** T-003 (entries are written there).
- **Status:** ☑ done

## After T-007 — pre-`/test` checklist

Run a full smoke pass before invoking `/test`:

- [ ] `rm template/local.db*` → `npm run dev` → DB badge green within 2 s.
- [ ] Quick-add an entry → appears on `/`.
- [ ] Quick-add same name with different macros → no duplicate food, latest macros stick on the food row, new entry uses new snapshot.
- [ ] Re-log via `/foods` → new entry on `/`.
- [ ] Update target on `/settings` → `/` progress bar denominator changes.
- [ ] Visit `/history` → 7 rows, today's row matches today's totals.
- [ ] Delete an entry on `/` → totals shrink.
- [ ] `npm run typecheck && npm run lint && npm run build` → all green.
- [ ] `grep -nE 'prepare\(.*\$\{|prepare\(.*\+' template/src` → zero hits.
- [ ] No committed `.claude/projects/` paths (`git log --stat --all -- .claude/projects/` → empty).

### T-008 — Cloud-mode port (queries.ts ↔ Supabase + Vercel preview deploy)

- **Goal:** make the calorie tracker actually deployable to Vercel by porting `queries.ts` to a router that picks SQLite or Supabase based on `resolveDriver()`. Local-mode behavior unchanged. Preview URL gets a real working demo.
- **Why this surfaced:** v1 was scoped local-only. `queries.ts` calls `getSqliteConnection()` directly per playbook §4 — that's `better-sqlite3` writing to `./local.db`, which Vercel's read-only filesystem won't allow. The user (2026-05-09) asked for a Vercel preview; cloud mode needs a real query implementation, not just RLS policies.
- **Files touched:**
  - `template/db/migrations/<ts>__cloud_anon_write_policies.sql` (new) — anon insert/update/delete on foods/entries/settings.
  - `template/db/migrations/<ts+1>__foods_name_lower_generated.sql` (new) — add `name_lower text generated always as (lower(name)) stored` to `foods` with a unique index, so Supabase `.upsert(..., { onConflict: 'name_lower' })` works.
  - SQLite siblings of both: anon-policy migration is a no-op stub (no RLS in SQLite); name_lower migration is also a no-op (the existing `unique index foods_name_lower_idx on foods (lower(name))` already enables case-insensitive upserts via `on conflict (lower(name))`).
  - `template/db/schema.sql` updated to reflect the policies + generated column.
  - `template/src/lib/db/queries-sqlite.ts` (new) — current `queries.ts` content moved verbatim.
  - `template/src/lib/db/queries-supabase.ts` (new) — Supabase implementations of the 9 functions.
  - `template/src/lib/db/queries.ts` — slim router using `resolveDriver()`.
  - `template/src/lib/db/tz.ts` (new) — single source of truth for the user TZ. Reads `NEXT_PUBLIC_TIMEZONE` env var; defaults to `"UTC"`.
  - `template/docs/deploy-vercel.md` (new) — step-by-step deploy guide.
- **TZ approach:** Cloud mode reads `NEXT_PUBLIC_TIMEZONE` (e.g. `Asia/Manila`) on the server and uses it to compute today/history boundaries. Local mode keeps server-local TZ behavior (single-user assumption holds).
- **Atomicity caveats:**
  - Cloud upsert+log is two sequential calls (`.upsert` food → `.insert` entry). Failure mode: food saved, entry not logged. User retries. Document.
  - Cloud re-log is two sequential calls (lookup food → insert entry). Same caveat.
  - SQLite path keeps single-transaction atomicity unchanged.
- **Acceptance criteria:**
  - [ ] `resolveDriver() === "sqlite"` route: every existing test + smoke behavior unchanged.
  - [ ] `resolveDriver() === "supabase"` route: today view, quick-add, foods library, re-log, settings, history, delete all work end-to-end on a real Supabase project.
  - [ ] No `'use client'` files added.
  - [ ] Build sizes per page within 10% of current (`/` ≤ 1.5 kB, others ≤ 200 B).
  - [ ] Local-mode tests still 44 passing.
  - [ ] Preview Vercel URL demonstrates a working full loop.
- **Verification:**
  - Local: `rm template/local.db* && npm run dev` → today view still seeds + renders.
  - Cloud: with `NEXT_PUBLIC_SUPABASE_URL` + keys set, `npm run dev` against the cloud DB works through the same UI.
  - Push branch `preview/calorie-tracker` → Vercel imports → preview URL boots → manual smoke.
- **Dependencies:** T-001 (schema base), T-007 (all features in place to port).
- **Status:** ☑ done (local); pending Vercel deploy verification

## Out-of-plan work

Anything discovered mid-build that wasn't in the spec — capture here, don't silently expand scope.

### OOP-1 — History view rebases historical days against the *current* daily target

Surfaced in `/review` (correctness axis, finding C2). `/history` reads `getDailyTarget()` once and applies that single value to every day's bar. If the user moves their target from 2000 → 2500, yesterday's 2100-cal day flips from red ("over") to green ("under") with no actual change in what they ate.

Three options:
- (a) Snapshot the active target on every `entries` row at log time (heaviest, but consistent with the FR-3 snapshot doctrine).
- (b) Add a `target_history` table recording target changes with `effective_at`; the history aggregation joins for the per-day target.
- (c) Accept current behavior as v1 — document that history bars are "vs current target" in the page copy.

For v1 the spec didn't promise per-day target snapshots and the demo path doesn't surface this. Defer until the user actually changes their target on a populated DB and complains.

### OOP-2 — Document the local-only no-auth security posture in SPEC.md

Surfaced in `/review` (security axis, finding S3). All four server actions are publicly invokable on `localhost:3000` — by design (single-user local app), but the spec's Privacy section should call out that the only barrier is same-origin browser convention. If `cloud` mode is ever activated, every write path will need an auth gate or anon-write RLS policy; the schema currently grants anon `read` only.
